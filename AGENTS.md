# 项目背景

使用`go-kratos`框架，`postgresQL`+`Redis`, `River` MQ, `gopay`。使用`sqlc`。再拓展OSS存储
电商平台。

## 实现功能

1. 用户数据存储和管理。需要存储商品浏览记录。
2. 商品和限时活动管理。
3. 用户分享的图文帖子，要求有点赞数据和评论（支持回复特定评论的楼中楼，仅嵌套 **1** 层）数据。

## 项目提醒

1. 没有历史数据，可以直接重写`migrations/`中的`init`SQL。
2. 微信支付仅作为桩实现，支付宝支付也仅支持沙箱环境。
