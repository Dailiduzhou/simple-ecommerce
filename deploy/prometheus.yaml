global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/payment-alerts.yaml

scrape_configs:
  - job_name: mall
    static_configs:
      - targets: [otel-collector:9464]
